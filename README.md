# xyz2dxf
This program reads an input file with 3D points in XYZ format, filters them by a minimum distance, optionally generates a regular grid, interpolates/extrapolates grid values using a fast bicubic (Catmull-Rom) routine, and writes the output to a DXF file.
